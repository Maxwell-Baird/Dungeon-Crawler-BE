language: python # => 1
    python: # => 2
      - "2.6"
      - "2.7"
    services: # => 3
      - postgres
    env: # => 4
      -DJANGO=1.8 DB=postgres
    install: # => 5
      - pip install -r requirements.txt
    before_script: # => 6
      - postgres -e 'create database test;' -u root
    script: # => 7
      - python manage.py test
